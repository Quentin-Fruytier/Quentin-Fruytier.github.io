# Local preview only. GitHub Pages builds the site remotely and ignores this
# file (it is in _config.yml's `exclude` list).
#
# To preview locally (requires Ruby):
#   gem install bundler
#   bundle install
#   bundle exec jekyll serve   # -> http://127.0.0.1:4000
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
